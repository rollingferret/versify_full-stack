const playlistDropdownReducer = (playlistDropdownState = {}, action) => {
    Object.freeze(playlistDropdownState);
    switch (action.type) {
        default:
            return playlistDropdownState;
    }
}

export default playlistDropdownReducer;    